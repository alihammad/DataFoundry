"""Metric computation over Gold/Silver (feature 006, T010/T018).

Compile a metric's declarative formula to a DuckDB query over Gold/Silver
datasets read through the gateway; identical results on both clouds.
"""
